import { emailCheck, formSubmit, logIn } from "../utilFunctions.js";
import { eMail, password, logInForm } from "../constants.js";

// logInForm.addEventListener("submit", logIn);

logInForm.addEventListener("submit", formSubmit);
