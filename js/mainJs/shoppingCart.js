import { shoppingCart, addCart, baseUrl } from "../constants.js";
import { printData } from "../utilFunctions.js";

// Calls printData directly as there is no need to fetch resources from the API.
printData();
